{
  "name": "basariyolu-auth-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for secure auth with HTTP-only cookies",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "keywords": ["cloudflare", "workers", "auth", "security"],
  "author": "BasariYolu",
  "license": "MIT",
  "dependencies": {
    "hono": "^3.11.7"
  },
  "devDependencies": {
    "wrangler": "^3.22.1"
  }
}
